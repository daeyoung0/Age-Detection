# 소비자 분석 시스템 — 실행 가이드 (RunPod GPU 버전)

폰 IP 카메라로 얼굴을 잡아 **연령·성별을 RunPod GPU에서 추론**하고,
**POS 결제 → 통계 대시보드**까지 연동하는 데모의 전체 실행 방법입니다.

---

## 0. 전체 구조 한눈에

```
 [폰 IP카메라]
      │  영상 스트림 (같은 와이파이)
      ▼
 [내 PC: camera_client.py] ──프레임(JPEG)──▶ [Cloudflare 터널] ──▶ [RunPod: infer_server.py (GPU)]
      │   ◀─────────────── age/gender 결과 ─────────────────────────────┘
      ├─▶ 영상 창에 초록 박스 + 라벨 그리기
      └─▶ POST /api/camera ─▶ [내 PC: backend_server.py :8000] ◀─ 읽기 ─ [내 PC: 프론트 :3000]
                                       ▲
                          POS 결제(/api/pay)가 최근 2초 카메라 결과와 매칭
```

| 위치 | 프로그램 | 포트 | 역할 |
|------|----------|------|------|
| **RunPod (GPU)** | `infer_server.py` | 8000 | 무거운 추론(RetinaFace+연령앙상블+FairFace) |
| RunPod | `cloudflared` 터널 | — | 8000을 인터넷에 공개 |
| 내 PC | `backend_server.py` | 8000 | DB 저장, POS/통계 API |
| 내 PC | `npm run dev` (프론트) | 3000 | POS 화면, 통계 대시보드 |
| 내 PC | `camera_client.py` | — | 카메라 캡처 + 화면 + RunPod 추론 요청 |

> **핵심:** GPU 추론만 RunPod에서, 카메라·화면·POS·통계는 전부 내 PC에서 돌아갑니다.

---

## 1. 사전 준비물 (최초 1회만)

- [x] 내 PC: `.venv`에 파이썬 의존성 설치됨 (이미 완료)
- [x] 내 PC: 프론트 `node_modules` 설치됨 (`npm install`, 이미 완료)
- [x] 내 PC: `runpodctl.exe` 다운로드됨 (이미 완료)
- [x] 배포 번들 `deploy_bundle.tar` 생성됨 (코드+모델, 이미 완료)
- [ ] 폰: **IP Webcam** 앱 설치 + 같은 와이파이 연결
- [ ] RunPod: GPU Pod 결제 (RTX A4000 / 3090 권장)

---

## 2. RunPod GPU 서버 띄우기

> 📌 Pod을 **새로 만들 때마다** 2-1 ~ 2-5를 다시 합니다.
> (Pod을 Stop 후 Start만 한 경우엔 파일이 남아 있으면 2-4부터)

### 2-1. Pod 접속
RunPod 콘솔 → **Pods → 내 Pod → Connect**
- 파일 받기/명령 실행: **"Jupyter Lab"(8888)** 또는 **"Open web terminal"**
- (Jupyter 터미널이 복사·붙여넣기가 편함: File → New → Terminal)

### 2-2. 코드+모델 전송 (`deploy_bundle.tar`)
**내 PC(PowerShell)** 에서:
```powershell
.\runpodctl.exe send deploy_bundle.tar
```
→ 출력된 코드 복사 (예: `6170-orion-magenta-iceberg-18`). **이 창은 켜둠.**

**Pod 터미널** 에서 (runpodctl 없으면 자동 설치):
```bash
cd /workspace
command -v runpodctl >/dev/null || { wget -qO /usr/local/bin/runpodctl https://github.com/runpod/runpodctl/releases/latest/download/runpodctl-linux-amd64 && chmod +x /usr/local/bin/runpodctl; }
runpodctl receive 6170-orion-magenta-iceberg-18
```
> 코드는 방금 복사한 걸로 교체. 831MB라 몇 분 걸립니다.

### 2-3. 압축 해제
```bash
tar -xf deploy_bundle.tar
ls
```
`infer_server.py`, `ensemble_korean.pt`, `src`, `fair_face_models`가 보이면 성공.

### 2-4. 의존성 설치 + 서버 실행
```bash
pip install timm retinaface-pytorch opencv-python-headless pillow numpy fastapi uvicorn pydantic
python -m uvicorn infer_server:app --host 0.0.0.0 --port 8000
```
아래까지 뜨면 정상 (**이 터미널은 끄지 말 것**):
```
[infer] device: cuda            ← cuda 여야 GPU 사용
  Loaded [swin] convnext_large
[infer] FairFace gender loaded
INFO:  Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

### 2-5. 인터넷 공개 (cloudflared 터널)
이 Pod은 8000 포트가 외부 노출이 안 돼 있어서 터널을 씁니다.
**Jupyter에서 새 터미널을 하나 더 열고**:
```bash
wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -O cloudflared && chmod +x cloudflared && ./cloudflared tunnel --url http://localhost:8000
```
출력 박스의 **공개 URL**을 메모 (예시):
```
https://vista-lighting-genealogy-castle.trycloudflare.com
```
> ⚠️ 이 URL은 **터널을 재시작할 때마다 바뀝니다.** 그때마다 새 URL을 써야 함.
> 이 터미널도 **끄지 말 것.**

### 2-6. 동작 확인
브라우저에서 `URL + /health` 열기:
```
https://vista-lighting-genealogy-castle.trycloudflare.com/health
→ {"device":"cuda","cuda":true,...}
```
이게 뜨면 RunPod 쪽 준비 끝.

---

## 3. 내 PC 실행 (터미널 3개)

> 폰 **IP Webcam 앱을 먼저 실행**(Start server)해서 같은 와이파이로 스트리밍 상태로 둡니다.
> 폰 화면의 주소(예: `http://192.168.219.101:8050`)를 확인하세요.

### 터미널 1 — 로컬 백엔드
```powershell
.\.venv\Scripts\python.exe -m uvicorn backend_server:app --host 0.0.0.0 --port 8000
```

### 터미널 2 — 프론트
```powershell
npm run dev
```
→ http://localhost:3000

### 터미널 3 — 카메라 클라이언트
`--infer-url`에 **2-5에서 받은 터널 URL + `/infer`** 를 넣습니다.
한 줄로 (backtick 쓰지 말 것):
```powershell
.\.venv\Scripts\python.exe camera_client.py --camera-url http://192.168.219.101:8050/video --infer-url https://vista-lighting-genealogy-castle.trycloudflare.com/infer --backend-url http://localhost:8000
```
- 영상 창 + 좌상단 **`GPU: online`** + 얼굴 초록 박스 + `young_adult / male (0.86)` 라벨이 뜨면 성공.
- 종료: 영상 창에서 **`q`**.

---

## 4. 사용법 (데모 시연)

1. 카메라가 손님 얼굴을 비추는 상태에서
2. http://localhost:3000/pos → 상품 선택 → 수량 → **결제**
   - 결제 시점 **최근 2초 내** 카메라가 보낸 연령/성별과 자동 매칭
3. http://localhost:3000/stats → 연령·성별 매출, 카테고리, 시간대 등 **5초마다 갱신**

> 더미 데이터로 대시보드를 미리 채우려면(시연 전):
> ```powershell
> .\.venv\Scripts\python.exe seed_data.py
> ```

---

## 5. 종료 / 재시작

### 종료
- 각 터미널에서 `Ctrl+C` (카메라 창은 `q`)
- **RunPod 과금 중단:** 콘솔에서 Pod **Stop**(볼륨은 유지·소액 과금) 또는 **Terminate**(완전 삭제)

### 다시 켤 때
- **Pod을 Stop만 했다 Start** → 파일은 남아있음. `cd /workspace` 후
  **2-4(서버 실행) + 2-5(터널)** 만 다시. (단, pip 패키지가 사라졌으면 2-4 설치부터)
- **Pod을 Terminate 했다 새로 생성** → 2-1부터 전부 다시.
- 터널 URL이 바뀌므로 **터미널 3의 `--infer-url`도 새 URL로** 교체.

---

## 6. 자주 나오는 문제

| 증상 | 원인 / 해결 |
|------|------------|
| 카메라 창이 안 뜸, `Failed to open camera` | 폰 IP Webcam 꺼짐/주소 다름 → 앱 Start, 폰 화면 주소 확인 후 `--camera-url` 수정 |
| 좌상단 `GPU: offline` | 터널 URL 틀림/만료 → 브라우저로 `/health` 확인, 2-5 URL 다시 확인 |
| 영상은 뜨는데 박스 없음, 라벨 `invalid_detections` | 얼굴이 작거나 흐릿/어두움 → 카메라에 얼굴 크고 또렷하게. 정상 동작임 |
| `infer] device: cpu` | Pod에 GPU 미할당 → GPU Pod인지 확인, 재생성 |
| POS 결제했는데 연령 `알 수 없음` | 결제 시점에 카메라가 얼굴을 안 잡고 있었음(최근 2초 규칙) → 얼굴 비추며 결제 |
| 통계가 안 변함 | 로컬 백엔드(터미널1) 꺼짐 → 다시 실행 |
| 폰이 자꾸 잠들어 끊김 | IP Webcam 설정에서 화면 꺼짐 방지/배터리 최적화 예외 ON |

---

## 7. 비용 관리 팁

- **안 쓸 땐 반드시 Pod Stop** (시간당 과금).
- 오래 안 쓸 거면 모델/코드는 이미 `deploy_bundle.tar`로 백업돼 있으니 **Terminate**해도 됨(2-1부터 다시 올리면 됨).
- GPU는 RTX A4000/3090 Community Cloud면 충분(시간당 $0.2 안팎). 더 비싼 건 불필요.

---

## 8. 파일 설명 (참고)

| 파일 | 위치 | 설명 |
|------|------|------|
| `infer_server.py` | RunPod | GPU 추론 API 서버 |
| `camera_client.py` | 내 PC | 카메라 캡처 + 화면 + RunPod 추론 요청 |
| `camera_infer_swin.py` | 공용 | 추론 함수 모음(서버가 재사용). 단독 실행 시 로컬 GPU/CPU 추론 |
| `backend_server.py` | 내 PC | POS/통계 백엔드(FastAPI+SQLite) |
| `seed_data.py` | 내 PC | 더미 데이터 생성 |
| `deploy_bundle.tar` | 내 PC | RunPod 업로드용 번들(코드+모델) |
| `preview_camera.py` | 내 PC | 추론 없이 카메라 영상만 미리보기(문제 진단용) |
| `app/`, `index.html` 등 | 내 PC | 프론트(Next.js) |
```
